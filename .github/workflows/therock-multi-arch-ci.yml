# Copyright Advanced Micro Devices, Inc.
# SPDX-License-Identifier: MIT

# Multi-Arch CI for rocm-systems
#
# This workflow replicates TheRock's multi-arch CI pipeline by calling
# TheRock's reusable workflows directly. It provides sharded, multi-stage
# builds with per-architecture testing.
#
# Key differences from therock-ci.yml:
# - Uses TheRock's multi-arch sharded build pipeline (foundation -> compiler-runtime -> math-libs)
# - Supports prebuilt stages to skip certain build stages
# - Better parallelization across GPU architectures

# TheRock ref: pinned to ROCm/TheRock commit 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac.
# NOTE: update all `@<ref>` refs and `ref:` inputs together when switching

# =============================================================================
# TEMPORARY: MI455 (gfx125X) bringup configuration
# TODO(geomin12): Remove this block once MI455 is fully enabled in therock-ci-config
#
# The gfx125x_family_overrides JSON configures MI455-specific test settings:
# - test-runs-on: Custom runner label for MI455 hardware
# - test_labels_for_family: Limit tests to hip-tests, rocrtst (sanity always runs)
# - Container options with ulimit for RCCL compatibility
# =============================================================================

name: TheRock Multi-Arch CI

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - labeled
      - unlabeled
  workflow_dispatch:
    inputs:
      linux_amdgpu_families:
        type: string
        description: "Insert comma-separated list of Linux GPU families to build and test. ex: gfx94X, gfx120X (empty = skip linux)"
        default: ""
      linux_test_labels:
        type: string
        description: "If enabled, reduce test set on Linux to the list of labels prefixed with 'test:'. ex: test:rocprim, test:hipcub"
        default: ""
      windows_amdgpu_families:
        type: string
        description: "Insert comma-separated list of Windows GPU families to build and test. ex: gfx94X, gfx120X (empty = skip windows)"
        default: ""
      windows_test_labels:
        type: string
        description: "If enabled, reduce test set on Windows to the list of labels prefixed with 'test:' ex: test:rocprim, test:hipcub"
        default: ""
      prebuilt_stages:
        type: string
        default: ""
        description: "Comma-separated build stages to skip (or 'all' for all stages); artifacts are copied from baseline_run_id instead"
      baseline_run_id:
        type: string
        default: ""
        description: "Workflow run ID to copy prebuilt stage artifacts from; required when prebuilt_stages is set"
      build_python_packages:
        type: boolean
        default: false
        description: "Build ROCm Python packages"
      build_pytorch:
        type: boolean
        default: false
        description: "Build PyTorch wheels"
      build_jax:
        type: boolean
        default: false
        description: "Build JAX wheels"
      build_native_linux:
        type: boolean
        default: false
        description: "Build native Linux packages and run install tests"
      baseline_repository:
        type: string
        default: ""
        description: "Repository to query for baseline_run_id. Defaults to github.repository for workflow_dispatch."

permissions:
  contents: read
  actions: read

concurrency:
  # A PR number if a pull request and otherwise the commit hash. This cancels
  # queued and in-progress runs for the same PR (presubmit) or commit
  # (postsubmit). The workflow name is prepended to avoid conflicts between
  # different workflows.
  group: ${{ github.workflow }}-${{ github.event.number || github.sha }}
  cancel-in-progress: >-
    ${{
      (github.event.action != 'labeled' && github.event.action != 'unlabeled') ||
      github.event.label.name == 'ci:multi-arch'
    }}

jobs:
  ci-gate:
    name: Check CI Label
    runs-on: ubuntu-24.04
    outputs:
      enabled: ${{ steps.gate.outputs.enabled }}
    steps:
      - name: Check for multi-arch opt-in
        id: gate
        run: |
          echo "enabled=${{ github.event_name != 'pull_request' || contains(github.event.pull_request.labels.*.name, 'ci:multi-arch') }}" >> "${GITHUB_OUTPUT}"

  configure:
    name: Configure Changed Projects
    needs: ci-gate
    if: ${{ needs.ci-gate.outputs.enabled == 'true' }}
    runs-on: ubuntu-24.04
    outputs:
      changed_projects: ${{ steps.configure-pr.outputs.changed_projects || steps.configure-manual.outputs.changed_projects }}
      run_all_tests: ${{ steps.configure-pr.outputs.run_all_tests || steps.configure-manual.outputs.run_all_tests }}
      skip_tests: ${{ steps.configure-pr.outputs.skip_tests || steps.configure-manual.outputs.skip_tests }}
      # Allowlist of build stages this PR needs (fed to TheRock's build_stages).
      build_stages: ${{ steps.build_stages.outputs.build_stages }}
    steps:
      - name: Checkout repository config
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          sparse-checkout: |
            .github/repos-config.json
            .github/scripts/compute_build_stages.py
          sparse-checkout-cone-mode: false

      - name: Checkout TheRock for configure script
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: ROCm/TheRock
          ref: 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
          path: _therock
          # _therock_utils + (cone-mode) root BUILD_TOPOLOGY.toml and
          # build_tools/project_mappings.json are needed to map changed projects
          # to build stages. Keep this ref in sync with the setup job's ref.
          sparse-checkout: |
            BUILD_TOPOLOGY.toml
            build_tools/github_actions
            build_tools/_therock_utils
            build_tools/project_mappings.json
            build_tools/artifact_subprojects.json
          sparse-checkout-cone-mode: false

      - name: Configure CI for pull request
        id: configure-pr
        if: ${{ github.event_name == 'pull_request' }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 _therock/build_tools/github_actions/configure_external_repo_ci.py \
            --event-name "${{ github.event_name }}" \
            --github-repo "${{ github.repository }}" \
            --base-sha "${{ github.event.pull_request.base.sha }}" \
            --head-sha "${{ github.event.pull_request.head.sha }}" \
            --config-path ".github/repos-config.json"

      - name: Configure CI for manual run
        id: configure-manual
        if: ${{ github.event_name != 'pull_request' }}
        run: |
          echo "changed_projects=" >> "${GITHUB_OUTPUT}"
          echo "run_all_tests=true" >> "${GITHUB_OUTPUT}"
          echo "skip_tests=false" >> "${GITHUB_OUTPUT}"

      # Map the changed projects to the minimal allowlist of TheRock build
      # stages needed to build them (BUILD_TOPOLOGY.toml is the source of truth;
      # this only reads it). Empty output = build all (fan-out / unknown project
      # / run_all_tests CI-infra change).
      - name: Compute build-stage allowlist
        id: build_stages
        run: |
          python3 .github/scripts/compute_build_stages.py \
            --changed-projects "${{ steps.configure-pr.outputs.changed_projects || steps.configure-manual.outputs.changed_projects }}" \
            --run-all-tests "${{ steps.configure-pr.outputs.run_all_tests || steps.configure-manual.outputs.run_all_tests }}" \
            --therock-path "_therock"

  setup:
    needs: [ci-gate, configure]
    if: ${{ needs.ci-gate.outputs.enabled == 'true' }}
    uses: ROCm/TheRock/.github/workflows/setup_multi_arch.yml@6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
    with:
      build_variant: "release"
      # Limit GPU families for rocm-systems CI
      # Note: gfx125X is added for MI455 bringup with limited tests (hip-tests, rocrtst, sanity)
      linux_amdgpu_families: ${{ inputs.linux_amdgpu_families || 'gfx94X,gfx950,gfx125X' }}
      windows_amdgpu_families: ${{ inputs.windows_amdgpu_families || 'gfx110X' }}
      linux_test_labels: ${{ inputs.linux_test_labels || '' }}
      windows_test_labels: ${{ inputs.windows_test_labels || '' }}
      # Per-PR selective build: the configure job maps the changed projects to
      # the minimal allowlist of build stages via TheRock's build topology.
      # TheRock (ROCm/TheRock#7600) builds only these stages and skips every
      # other stage outright (no build, no artifact copy). Empty = build all,
      # the fail-safe for fan-out / unknown-project / CI-infra changes.
      build_stages: ${{ needs.configure.outputs.build_stages }}
      prebuilt_stages: ${{ inputs.prebuilt_stages || '' }}
      baseline_run_id: ${{ inputs.baseline_run_id || '' }}
      build_python_packages: ${{ inputs.build_python_packages || false }}
      build_pytorch: ${{ inputs.build_pytorch || false }}
      build_jax: ${{ inputs.build_jax || false }}
      build_native_linux: ${{ inputs.build_native_linux || false }}
      baseline_repository: ${{ inputs.baseline_repository || (inputs.baseline_run_id != '' && github.repository) || '' }}
      # Enable automatic stage reuse - TheRock finds commit-compatible baselines
      # and determines which stages to rebuild based on changed files
      stage_reuse_mode: reuse-stage
      repository: ROCm/TheRock
      ref: 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
      # TEMPORARY: MI455 bringup - pass family_overrides to configure test runner and limited tests
      # TODO(geomin12): Remove family_overrides once MI455 is in therock-ci-config
      # NOTE: MI455 machine is down - test-runs-on set to "" until back online (was: linux-mi455-gpu-rocm)
      external_repo: >-
        {
          "repository": "${{ github.repository }}",
          "ref": "${{ github.sha }}",
          "family_overrides": {
            "gfx125x": {
              "linux": {
                "test-runs-on": "",
                "test_labels_for_family": ["test:hip-tests", "test:rocrtst"],
                "fetch-gfx-targets": ["gfx1250"]
              }
            }
          }
        }

  linux_build_and_test:
    name: Linux::${{ fromJSON(needs.setup.outputs.linux_build_config || '{}').build_variant_label || 'skip' }}
    needs: [configure, setup]
    if: >-
      ${{
        needs.setup.outputs.linux_build_config != '' &&
        needs.setup.outputs.enable_build_jobs == 'true' &&
        needs.configure.outputs.skip_tests != 'true'
      }}
    uses: ROCm/TheRock/.github/workflows/multi_arch_ci_linux.yml@6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
    with:
      build_config: ${{ needs.setup.outputs.linux_build_config }}
      test_labels: ${{ needs.setup.outputs.linux_test_labels }}
      rocm_package_version: ${{ needs.setup.outputs.rocm_package_version }}
      rocm_deb_package_version: ${{ needs.setup.outputs.rocm_deb_package_version }}
      rocm_rpm_package_version: ${{ needs.setup.outputs.rocm_rpm_package_version }}
      test_type: ${{ needs.setup.outputs.test_type }}
      external_repo_config: ${{ needs.setup.outputs.external_repo_config }}
      # Empty string when run_all_tests=true triggers full test run in downstream workflow
      changed_projects: ${{ needs.configure.outputs.run_all_tests == 'true' && '' || needs.configure.outputs.changed_projects }}
      repository: ROCm/TheRock
      ref: 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
    permissions:
      contents: read
      id-token: write

  windows_build_and_test:
    name: Windows::${{ fromJSON(needs.setup.outputs.windows_build_config || '{}').build_variant_label || 'skip' }}
    needs: [configure, setup]
    if: >-
      ${{
        needs.setup.outputs.windows_build_config != '' &&
        needs.setup.outputs.enable_build_jobs == 'true' &&
        needs.configure.outputs.skip_tests != 'true'
      }}
    uses: ROCm/TheRock/.github/workflows/multi_arch_ci_windows.yml@6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
    with:
      build_config: ${{ needs.setup.outputs.windows_build_config }}
      test_labels: ${{ needs.setup.outputs.windows_test_labels }}
      rocm_package_version: ${{ needs.setup.outputs.rocm_package_version }}
      test_type: ${{ needs.setup.outputs.test_type }}
      external_repo_config: ${{ needs.setup.outputs.external_repo_config }}
      # Empty string when run_all_tests=true triggers full test run in downstream workflow
      changed_projects: ${{ needs.configure.outputs.run_all_tests == 'true' && '' || needs.configure.outputs.changed_projects }}
      repository: ROCm/TheRock
      ref: 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
    permissions:
      contents: read
      id-token: write

  multi_arch_ci_summary:
    name: Multi-Arch CI Summary
    if: always()
    needs:
      - ci-gate
      - configure
      - setup
      - linux_build_and_test
      - windows_build_and_test
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout TheRock repository
        if: ${{ needs.ci-gate.result == 'success' && needs.ci-gate.outputs.enabled == 'true' }}
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          repository: "ROCm/TheRock"
          ref: 6b8d96a0f5e5cbaedc622c13773a915af2d3aaac # 2026-09-11
          sparse-checkout: build_tools/github_actions
          sparse-checkout-cone-mode: true

      - name: Evaluate workflow results
        if: ${{ needs.ci-gate.result == 'success' && needs.ci-gate.outputs.enabled == 'true' }}
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          python3 build_tools/github_actions/workflow_summary.py \
            --needs-json '${{ toJSON(needs) }}'

      - name: Report skipped workflow
        if: ${{ needs.ci-gate.result == 'success' && needs.ci-gate.outputs.enabled == 'false' }}
        run: |
          echo "TheRock Multi-Arch CI is skipped by default for pull requests."
          echo "Add the ci:multi-arch label to enable it."

      - name: Report gate failure
        if: ${{ needs.ci-gate.result != 'success' }}
        run: |
          echo "TheRock Multi-Arch CI gate did not complete successfully."
          exit 1
